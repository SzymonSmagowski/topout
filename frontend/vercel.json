{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "ln -sf ../frontend/node_modules ../convex/node_modules && pnpm exec convex deploy --cmd 'pnpm run build'",
  "outputDirectory": ".next"
}
